#!/usr/bin/python3

def max_integer(my_list=[]):
    if len(my_list) == 0:
        _max = None
    else:
        _max = my_list[0]
        for i in my_list:
            if i >= max:
                i = max
    return _max
